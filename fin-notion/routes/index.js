import {createRouter, createWebHistory} from "vue-router"

export default({
    history : createWebHistory(), // HTML5 mode
    scrollBegavior : () => ({top : 0, left : 0}),
    routes : {
        
    }
})